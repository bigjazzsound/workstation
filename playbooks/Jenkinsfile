pipeline {
    agent {
        node {
            label 'desktop'
        }
    }
    stages {
        stage('build') {
            steps {
                ansiblePlaybook('main.yml') {
                    inventoryPath('hosts.yml')
                    limit('local')
                    colorized(true)
                }
            }
        }
    }
}
