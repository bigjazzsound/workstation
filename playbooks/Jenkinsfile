pipeline {
    agent {
        node {
            label 'desktop'
        }
    }
    stages {
        stage('build') {
            steps {
                ansiblePlaybook(
                    playbook: 'main.yml')
            }
        }
    }
}
