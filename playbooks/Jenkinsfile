pipeline {
    agent { 
        node { label 'desktop' } 
    }
    stages {
        stage('build') {
            steps {
                ansiColor('xterm') {
                    ansiblePlaybook(
                        playbook: 'main.yml',
                        inventory: 'hosts.yml',
                        colorized: true)
                }
            }
        }
    }
}
